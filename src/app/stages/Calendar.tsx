"use client";

import { useState } from 'react';

export default function Calendar() {
  // logiques du calendrier côté client
  const [month, setMonth] = useState(11);
  // ...

  return (
    <div>
      {/* rendu du calendrier */}
    </div>
  );
}
